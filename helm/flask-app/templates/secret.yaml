{{- if .Values.secret.enabled }}

apiVersion: v1
kind: Secret

metadata:
  name: {{ include "flask-app.fullname" . }}

type: Opaque

stringData:
{{- range $key, $value := .Values.secret.data }}
  {{ $key }}: "{{ $value }}"
{{- end }}

{{- end }}
