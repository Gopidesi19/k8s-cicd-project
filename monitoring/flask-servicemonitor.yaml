apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: flask-app
  namespace: monitoring

spec:
  selector:
    matchLabels:
      app: flask-app

  namespaceSelector:
    any: true

  endpoints:
    - port: http
      path: /metrics
      interval: 30s
